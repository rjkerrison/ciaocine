<h2>{{appTitle}}</h2>
<p>{{pageSubtitle}}</p>
<div class='random-screening'>
  {{#with showtime}}
    <h2>
      <a href='/cinema/{{cinema.id}}'>
        {{cinema.name}}
      </a>
    </h2>
    <p>
      Why not see
      <strong>{{movie.title}}</strong>
      at
      <strong>{{time startTime}}</strong>
      on
      <strong>{{date startTime}}</strong>?
    </p>
    <picture><img src='{{movie.poster}}' alt='{{movie.title}}' /></picture>
  {{/with}}
</div>