<section>
{{#with cinema}}
  <h2>Showtimes for {{name}}</h2>
  <p>
    {{address}},
    {{city}}
    {{zipcode}}
  </p>
  {{> favouriteCinema cinema=this liked=liked}}
  <ul>
    {{#each member_cards}}
      <li data-code='{{code}}'>
        {{label}}
      </li>
    {{/each}}
  </ul>
  <p>
    <a href='/cinema/{{_id}}/byDate'>
      View listings by date
    </a>
  </p>
{{/with}}

<nav>
  {{> moviesFilters label='sur le' urls=calendarUrls}}
  {{> moviesFilters label='à partir de' urls=hoursUrls}}
</nav>

<div class='movies'>
  {{#each movies}}
    <div class='movie overlay-container expander-container'>
      {{> movieHeading movie=movie}}
      {{> movieShowtimes showtimes=showtimes showDate=true}}
    </div>
  {{/each}}
</div>
</section>